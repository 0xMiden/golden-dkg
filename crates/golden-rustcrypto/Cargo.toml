[package]
name = "golden-rustcrypto"
version = "0.2.0"
description = "RustCrypto group adapters for Golden distributed key generation"
edition.workspace = true
homepage.workspace = true
license.workspace = true
readme = "README.md"
repository.workspace = true
rust-version.workspace = true
publish = ["crates-io"]

[dependencies]
golden-core = { workspace = true }
k256 = { workspace = true, default-features = false, features = ["arithmetic", "hash2curve"], optional = true }
p256 = { version = "0.13", default-features = false, features = ["arithmetic", "hash2curve"], optional = true }
rand_core = { workspace = true }
sha2 = { workspace = true }
subtle = { workspace = true }
zeroize = { workspace = true }

[features]
default = []
p256 = ["dep:p256"]
k256 = ["dep:k256"]

[dev-dependencies]
hex = { workspace = true }
rand_chacha = { workspace = true }

[lints]
workspace = true
